# Veeam-PostgreSQL-Updater
Script that can be run against a VBR or VBO/VBM server, disable jobs and service, update PostgreSQL and then enable everything again. Designed to be run manually or from RMM.
